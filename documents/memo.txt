Sequence（シーケンス）
Parallel Split（並列分岐）
Synchronization（同期）
Exclusive Choice（排他的選択）
Simple Merge（単純結合）
Multi-Choice（多重選択）
Synchronizing Merge（同期結合）
Multiple Merge（多重結合）
Discriminator（判別結合）
Arbitrary Cycles（任意のループ）
Implicit Termination（暗黙的終了）
Multiple Instances Without Synchronization（同期なし複数インスタンス）
Multiple Instances With a Priori Design-Time Knowledge（事前定義インスタンス数あり）
Multiple Instances With a Priori Runtime Knowledge（実行時にインスタンス数決定）
Multiple Instances Without a Priori Runtime Knowledge（インスタンス数無制限）
Deferred Choice（保留選択）
Interleaved Parallel Routing（交互並列ルーティング）
Milestone（マイルストーン）
Cancel Activity（アクティビティのキャンセル）
Cancel Case（ケース全体のキャンセル）
Structured Loop（構造化ループ）
Recursion（再帰）
Transient Trigger（トランジェントトリガー）


to_dict()にexcludeパラメータとか付けると、秘密情報・一時情報を抜いて保存も可

apply_patch_to_activitiesでPK/unique対応に切り替えたくなったら
index→idフィールドにするだけでOK

差分をMermaidやテーブル化してUIで見せると、管理者ウケ抜群

差分適用の「ログ」もApplicationSnapshotに履歴として残していくと「誰が何をどう直したか」も一発で見れる

🎯 推奨運用イメージ
自動保存バッチ：全申請に一定タイミングでスナップショット自動保存

ユーザ操作UI：「スナップショット一覧」＋「復元」「部分復元」「差分確認」「差分パッチ適用」ボタン

監査・事故対応：管理画面から過去の状態・変更履歴を一瞬で再現、ロールバック

開発/QA：「本番で困ったデータをそのまま開発にコピー」→再現バグ撲滅


